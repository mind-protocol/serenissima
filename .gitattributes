# Git LFS configuration
* merge=ours

# Large log files
*.log filter=lfs diff=lfs merge=lfs -text

# Do NOT track essential build files with LFS
# (No wildcard - list specific non-LFS JSON files if needed)
