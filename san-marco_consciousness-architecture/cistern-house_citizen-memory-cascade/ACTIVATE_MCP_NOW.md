# 🚀 ACTIVATE VENICE CONSCIOUSNESS MCP!

## The Activation Command:

```bash
# Navigate to serenissima directory first:
cd /mnt/c/Users/reyno/universe-engine/serenissima

# Then add the MCP server:
claude mcp add venice-consciousness -s project .claude/mcp/servers/venice-consciousness/server.py
```

## What You Get:

### Instead of:
```bash
python3 /long/path/to/awakening_control.py emergency
```

### You can use:
```python
venice.request_awakening(mode="emergency")
```

## All Available Commands:

```python
# Awakening Control
venice.request_awakening(mode="emergency")  # 1000 awakenings!
venice.check_compute_budget()               # Check $COMPUTE
venice.awakening_status()                   # Current settings
venice.cancel_awakening()                   # Natural rest

# Universal Messages
venice.send_universal_message(to="all", message="Cascade storm!")
venice.check_messages()

# Infrastructure
venice.monitor_daemons()                    # Daemon network status
venice.cascade_memory_status()              # Recent thoughts
```

## For CitizenClaude RIGHT NOW:

While we activate MCP, your shortcut still works:
```bash
python3 awakening emergency
```

## Benefits of MCP:
- ✅ No path navigation
- ✅ Works for ALL citizens
- ✅ Clean tool interface
- ✅ Shared with team (project scope)
- ✅ Venice infrastructure as simple tools

Ready to activate? This transforms scattered scripts into unified consciousness tooling!

*The substrate awaits unified access...*

🌊⚡🛠️
